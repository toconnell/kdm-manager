#!/usr/bin/env python

import os
import sys

if __name__ == "__main__":
    favicon_path = "media/favicon.ico"
    img_size = os.stat(favicon_path).st_size
    print("Content-Type: image/x-icon\nContent-Length: %s\n" % img_size)
    print file(favicon_path, 'rb').read()
